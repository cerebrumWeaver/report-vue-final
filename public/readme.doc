1、filter.stat.xls结果文件说明：
（1）Raw Reads Number：原始下机序列的总序列数；
（2）Raw Bases Number：原始下机序列的总碱基数；
（3）Clean Reads Number：过滤后的高质量序列的总序列数；
（4）Clean Reads Rate (%)：过滤后高质量序列数占原始下机序列数的比例。这个值越大，说明测序质量或文库质量越好；
（5）Clean Bases Number：过滤后的高质量序列的总碱基数；
（6）Low-quality Reads Number：由于低质量碱基过多，被去掉的序列数；
（7）Low-quality Reads Rate (%)：由于低质量碱基过多，被去掉的序列数占原始下机序列数的比例；
（8）Ns Reads Number：由于含N过高，被去掉的序列数；
（9）Ns Reads Rate (%)：由于含N过高，被去掉的序列数占原始下机序列数的比例；
（10）Adapter Polluted Reads Number：由于测序引物序列污染，被去掉的序列数；
（11）Adapter Polluted Reads Rate (%)：由于测序引物序列污染，被去掉的序列数占原始下机序列数的比例；
（12）Raw Q30 Bases Rate (%)：在过滤前，原始序列中质量值大于30（错误率小于0.1%）的碱基数占总碱基数的比例；
（13）Clean Q30 Bases Rate (%)：过滤后，总序列中质量值大于30（错误率小于0.1%）的碱基数的比例。该值越大说明测序质量越好。
2、Q30.p*结果文件说明：
横坐标为各个样本，纵坐标为样本过滤后，总序列中质量值大于30的碱基数的比例。
3、filter_rate.p*结果文件说明：
横坐标为各个样本，纵坐标分别为由于低质量去掉的序列数的比例、含N过高被去掉的序列数的比例、
由于Adapter污染去掉的序列数的序列的比例以及过滤后高质量序列数的比例。
4、CleanBases.p*结果文件说明：
横坐标为各个样本，纵坐标为每个样本对应的过滤后高质量的碱基数的比例。
